SELECT COUNT(*)
FROM bi_schema.fact_annonces;

SELECT *
FROM bi_schema.fact_annonces
LIMIT 10;

SELECT *
FROM bi_schema.dim_date
LIMIT 10;

SELECT *
FROM bi_schema.dim_localisation
LIMIT 10;

SELECT *
FROM bi_schema.dim_bien
LIMIT 10;